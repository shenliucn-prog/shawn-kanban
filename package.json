{
  "name": "kindle-assistant",
  "version": "0.1.0",
  "description": "Cross-platform (Windows/macOS) data service that turns a jailbroken Kindle into an e-ink assistant dashboard: WorkBuddy / Claude Code / Codex quotas, weather, US & A-share markets, world clocks and FX — served over LAN to the KOReader KindleDash plugin.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "pretest": "node test/fixtures/build.mjs",
    "test": "node --test",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "better-sqlite3": "^12.0.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
